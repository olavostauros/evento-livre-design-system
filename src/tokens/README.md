# Tokens

Single source of truth for design values. `tailwind.config.ts` reads from here.

Expected files (create as decisions are accepted):

- `colors.ts`     placeholder names only until 0001 is accepted
- `typography.ts` until 0004 is accepted
- `spacing.ts`
- `radii.ts`
- `elevation.ts`
- `motion.ts`     durations and easings, until 0003 is accepted
- `index.ts`      re-exports
