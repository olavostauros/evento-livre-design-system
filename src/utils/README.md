# Utils

Small helpers shared by tokens, tests and components.

- `cn.ts`     `cn(...classes)`: join class names, drop falsy values. No merge logic.
- `color.ts`  WCAG luminance and contrast maths, AA thresholds.
